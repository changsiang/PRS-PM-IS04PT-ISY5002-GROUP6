import { RightDndDirective } from './dnd.directive';

describe('DndDirective', () => {
  it('should create an instance', () => {
    const directive = new RightDndDirective();
    expect(directive).toBeTruthy();
  });
});
